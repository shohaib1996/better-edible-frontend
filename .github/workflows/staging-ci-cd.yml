name: Build and Deploy Staging

on:
  push:
    branches:
      - staging

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/shohaib1996/better-edible-frontend:staging
            ghcr.io/shohaib1996/better-edible-frontend:staging-${{ github.sha }}
          build-args: |
            NEXT_PUBLIC_ENV=staging

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to staging server
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 143.244.182.86
          username: deploy
          key: ${{ secrets.PROD_SSH_KEY }}
          script: |
            cd /opt/apps/better-edibles
            docker pull ghcr.io/shohaib1996/better-edible-frontend:staging
            docker compose -f docker-compose.staging.yml up -d --force-recreate staging-frontend
